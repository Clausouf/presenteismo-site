import DashboardBase from '@/components/DashboardBase'; // ou o caminho correto onde está o componente base

export default function ConsolidadoPage() {
  return <DashboardBase tipo="consolidado" />;
}
